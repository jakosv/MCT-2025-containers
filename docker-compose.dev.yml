services:
  restbed-app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    volumes:
      - ./src:/app/src:ro
    environment:
      - DEV=1
      - LOG_LEVEL=info
    restart: unless-stopped
